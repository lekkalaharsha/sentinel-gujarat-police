fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
requests==2.32.3
opencv-python==4.10.0.84
pydantic==2.9.2

# Real analytics deps — see requirements-ml.txt. main.py tries to construct
# YoloVehicleDetector/PaddleOcrPlateReader at startup and falls back to the
# honest no-op stubs (with a loud warning) if these aren't installed, so the
# service still runs without them — just without real detection/ANPR.
