# AGENTS.md

This repo keeps a single instructions file to avoid the two drifting apart.

**Read `CLAUDE.md`** in this same directory — it covers everything: the
hackathon context, architecture decisions, the actual stack/tooling,
build/test commands, naming/database/API conventions, security rules,
and the git workflow for this repo.
